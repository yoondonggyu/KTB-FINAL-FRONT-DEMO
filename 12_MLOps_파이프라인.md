# MLOps 및 배포 파이프라인

> AI 서버의 모델 배포, 실험 관리, 파이프라인 자동화 계획

---

## 📚 목차

- [1. 개요](#1-개요)
- [2. 현재 인프라 구조](#2-현재-인프라-구조)
- [3. CI/CD 파이프라인](#3-cicd-파이프라인)
- [4. 향후 MLOps 도입 계획](#4-향후-mlops-도입-계획)
- [5. 조사 필요 항목](#5-조사-필요-항목)
- [6. PL 미팅 피드백](#6-pl-미팅-피드백)

---

## 1. 개요

### 1.1. 현재 상황

우리 AI 서비스는 **자체 모델 학습 없이 외부 LLM API(Gemini)를 사용**하는 구조입니다.

| 구분 | 현재 상태 | 비고 |
|------|----------|------|
| **모델 학습** | ❌ 없음 | Gemini API 사용 |
| **파인튜닝** | ❌ 없음 | 추후 고려 |
| **배포** | Docker + 수동 배포 | |
| **실험 추적** | ❌ 없음 | 추후 도입 예정 |

### 1.2. 왜 MLOps가 필요한가?

현재 구조에서도 DAG 기반 배치 작업이 필요한 상황이 있을 수 있습니다:

| 상황 | 필요성 |
|------|-------|
| VectorDB 최적화 | 정기적 인덱스 재구축 |
| 사용량 통계 집계 | 일별/주별 리포트 생성 |
| 프롬프트 A/B 테스트 | 버전별 성능 비교 |
| 캐시 정리 | 오래된 응답 캐시 삭제 |

---

## 2. 현재 인프라 구조

### 2.1. 서빙 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          현재 AI Server 배포 구조                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   [GitHub]                                                                  │
│       │                                                                     │
│       ▼                                                                     │
│   ┌──────────────┐                                                          │
│   │   GitHub     │                                                          │
│   │   Actions    │                                                          │
│   │   (CI)       │                                                          │
│   └──────┬───────┘                                                          │
│          │                                                                  │
│          ▼                                                                  │
│   ┌──────────────┐    ┌──────────────┐                                      │
│   │   Docker     │───▶│  Docker Hub  │                                      │
│   │   Build      │    │  (Registry)  │                                      │
│   └──────────────┘    └──────┬───────┘                                      │
│                              │                                              │
│                              ▼                                              │
│                       ┌──────────────┐                                      │
│                       │   Compute    │                                      │
│                       │   (GCP/AWS)  │                                      │
│                       └──────┬───────┘                                      │
│                              │                                              │
│                              ▼                                              │
│                       ┌──────────────┐                                      │
│                       │  FastAPI +   │                                      │
│                       │  Uvicorn     │                                      │
│                       └──────────────┘                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2. 구성 요소

| 구성 요소 | 기술 | 역할 |
|----------|------|------|
| **Web Framework** | FastAPI | API 서버 |
| **ASGI Server** | Uvicorn | 비동기 서버 |
| **Containerization** | Docker | 환경 격리 |
| **Registry** | Docker Hub | 이미지 저장소 |
| **Compute** | GCP / AWS | 클라우드 컴퓨팅 |
| **VectorDB** | ChromaDB | 임베딩 저장 |

---

## 3. CI/CD 파이프라인

### 3.1. 현재 CI 흐름

```yaml
# .github/workflows/ai-server-ci.yml (요약)
name: AI Server CI

on:
  push:
    branches: [main, develop, release/*]
  pull_request:
    branches: [main, develop, release/*]

jobs:
  lint:
    # Ruff + Black 검사
    
  unit-test:
    # pytest -m unit
    
  integration-test:
    # pytest -m integration (release/main만)
    
  build:
    # Docker 이미지 빌드
    
  notify:
    # Discord 알림
```

### 3.2. CD 흐름 (계획)

| 단계 | 현재 | 계획 |
|------|------|------|
| **빌드** | GitHub Actions | 유지 |
| **레지스트리** | Docker Hub | 유지 (또는 GCR) |
| **배포** | 수동 | 자동화 (ArgoCD 또는 kubectl) |
| **롤백** | 수동 | 자동화 |

---

## 4. 향후 MLOps 도입 계획

### 4.1. 도입 로드맵

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        MLOps 도입 로드맵                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   [MVP - 현재]                                                              │
│   └── 수동 배포, GitHub Actions CI                                          │
│                                                                             │
│   [v2 - 1차 고도화] ⭐ 경량 MLOps 도입                                        │
│   ├── Prefect 또는 Airflow: 배치 작업 스케줄링                                │
│   ├── MLflow: 프롬프트 버전 추적, 성능 비교                                    │
│   └── 정기 VectorDB 최적화 파이프라인                                         │
│                                                                             │
│   [v3 이후 - 고급] 본격 MLOps                                                 │
│   ├── Kubeflow (자체 모델 학습 시)                                           │
│   ├── Feature Store                                                        │
│   └── Model Monitoring                                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2. 도구별 도입 시기

| 도구 | 용도 | 도입 시기 | 필요 조건 |
|------|------|----------|----------|
| **Prefect** | 워크플로우 오케스트레이션 | v2 (2차 배포) | 배치 작업 필요 시 |
| **Airflow** | DAG 기반 파이프라인 | v2 (대안) | 배치 작업 필요 시 |
| **MLflow** | 실험 추적, 모델 레지스트리 | v2 | 프롬프트 A/B 테스트 시 |
| **Kubeflow** | ML 파이프라인 | v3 이후 | 자체 모델 학습 시 |
| **Weights & Biases** | 실험 추적 (SaaS) | v2 (대안) | 간편한 설정 원할 시 |

### 4.3. Airflow vs Kubeflow 비교

| 항목 | Airflow | Kubeflow |
|------|---------|----------|
| **주 용도** | 범용 워크플로우 스케줄링 | ML 특화 파이프라인 |
| **학습 곡선** | 🟡 중간 | 🔴 높음 |
| **Kubernetes 필수** | ❌ (옵션) | ✅ (필수) |
| **우리 서비스 적합도** | ✅ 적합 (배치 작업) | ⚠️ 오버엔지니어링 |
| **MLOps 직무 인지도** | ✅ 높음 | ✅ 높음 |

### 4.4. 현재 서비스에 Airflow 도입 시 활용 예시

```python
# DAG 예시: VectorDB 최적화
from airflow import DAG
from airflow.operators.python import PythonOperator
from datetime import datetime, timedelta

default_args = {
    'owner': 'ai-team',
    'retries': 1,
    'retry_delay': timedelta(minutes=5),
}

with DAG(
    'vectordb_optimization',
    default_args=default_args,
    schedule_interval='0 3 * * *',  # 매일 새벽 3시
    start_date=datetime(2026, 2, 1),
) as dag:
    
    def optimize_vectordb():
        # ChromaDB 인덱스 최적화
        pass
    
    def generate_usage_report():
        # 일간 사용량 통계 생성
        pass
    
    def cleanup_old_cache():
        # 7일 이상 된 응답 캐시 삭제
        pass
    
    optimize = PythonOperator(
        task_id='optimize_vectordb',
        python_callable=optimize_vectordb,
    )
    
    report = PythonOperator(
        task_id='generate_report',
        python_callable=generate_usage_report,
    )
    
    cleanup = PythonOperator(
        task_id='cleanup_cache',
        python_callable=cleanup_old_cache,
    )
    
    optimize >> report >> cleanup
```

---

## 5. 조사 필요 항목

### 5.1. 4주차 조사 예정 (클라우드 연계)

| 항목 | 조사 내용 | 담당 |
|------|----------|------|
| **비용 산정** | GCP/AWS LLM API 비용, 컴퓨팅 비용 | 클라우드 + AI |
| **서버리스 검토** | Cloud Functions / Lambda로 API 서빙 가능 여부 | 클라우드 |
| **VectorDB 배포** | ChromaDB vs Pinecone (관리형) 비용 비교 | AI |
| **임베딩 모델** | 어떤 임베딩 모델 사용할지 결정 | AI |

### 5.2. 성능 지표 참고 (이전 기수 WIKI)

| 지표 유형 | 조사 항목 |
|----------|----------|
| **응답 속도** | LLM API 응답 시간, TTFT |
| **정확도** | 분석 결과 품질, 면접 질문 적절성 |
| **비용** | 토큰당 비용, 월간 예상 비용 |
| **처리량** | 초당 처리 요청 수 (RPS) |

---

## 6. PL 미팅 피드백

### 6.1. 피드백 요약 (3주차)

| 질문 | PL 답변 |
|------|--------|
| **아키텍처 설계 범위?** | 3주차 기준으로 AI(LangChain) 부분만 그려도 됨 |
| **클라우드 연계?** | 다음 주(4주차)에 비용 포함해서 본격적으로 진행 |
| **성능 지표?** | 이전 기수 WIKI 참고 (응답속도, 정확도 등) |
| **VectorDB 저장 방식?** | 데이터 구조 고민 필요 (임베딩 모델 선택 포함) |
| **서버리스 가능성?** | API를 서버리스로 구성하는 것 고려 |

### 6.2. 즉시 반영 사항

| 항목 | 상태 | 비고 |
|------|:----:|------|
| 이전 기수 WIKI 확인 | 🔄 진행중 | 성능 지표 참고 |
| 사용 모델 결정 | ✅ 완료 | Gemini 1.5 Flash/Pro |
| 성능 지표 정의 | 🔄 진행중 | 성능_최적화.md 참고 |
| VectorDB 데이터 구조 | 🔄 진행중 | 임베딩 모델 선택 후 |

### 6.3. 4주차 진행 예정

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          4주차 조사 예정 항목                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   1. 클라우드 비용 산정                                                       │
│      ├── GCP Compute 비용                                                   │
│      ├── Gemini API 월간 예상 비용                                           │
│      ├── VectorDB 호스팅 비용 (ChromaDB vs Pinecone)                         │
│      └── 서버리스 vs 전용 서버 비용 비교                                       │
│                                                                             │
│   2. 임베딩 모델 결정                                                         │
│      ├── Gemini text-embedding-004                                          │
│      ├── OpenAI text-embedding-3-small                                      │
│      └── 오픈소스 (sentence-transformers)                                    │
│                                                                             │
│   3. 아키텍처 다이어그램 정교화                                                 │
│      ├── 클라우드 인프라 포함                                                 │
│      └── 비용 효율적인 구조 설계                                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 참고 자료

- [Airflow 공식 문서](https://airflow.apache.org/docs/)
- [Kubeflow 공식 문서](https://www.kubeflow.org/docs/)
- [MLflow 공식 문서](https://mlflow.org/docs/latest/index.html)
- [Prefect 공식 문서](https://docs.prefect.io/)
- [09_성능_최적화.md](09_성능_최적화.md)
- [10_테스트_전략.md](10_테스트_전략.md)
